{
  "name": "cp-groups",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "bootstrap": "wpackio-scripts bootstrap",
    "build": "wpackio-scripts build && npm run build:blocks",
    "start": "wpackio-scripts start",
    "archive": "wpackio-scripts pack",
    "dev:blocks": "wp-scripts start --webpack-src-dir=blocks/ --output-path=dist/blocks",
    "build:blocks": "wp-scripts build --webpack-src-dir=blocks/ --output-path=dist/blocks"
  },
  "devDependencies": {
    "@wordpress/scripts": "^26.9.0",
    "@wpackio/scripts": "^6.4.0",
    "sass": "^1.42.1"
  },
  "dependencies": {
    "@wordpress/icons": "^9.29.0",
    "@wpackio/entrypoint": "^6.4.0",
    "classnames": "^2.3.2",
    "sass": "^1.42.1",
    "url-loader": "^4.1.1"
  }
}
